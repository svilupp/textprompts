"""Internal syntax engine for textprompts conditional bodies (SPEC v2).

This subpackage is internal. External callers should use
``textprompts.load_prompt`` / ``Prompt.format`` — not these modules directly.
"""

from __future__ import annotations
