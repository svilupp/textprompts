{
  "name": "textprompts",
  "version": "0.2.0",
  "description": "TypeScript companion to textprompts for loading and formatting prompt files.",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.mjs",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    },
    "./cli": {
      "import": "./dist/cli.mjs",
      "require": "./dist/cli.cjs",
      "types": "./dist/cli.d.ts"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "rm -rf dist && bun run build:esm && bun run build:cjs && bun run build:types",
    "build:esm": "bun build src/index.ts src/cli.ts --target node --format esm --outdir dist --entry-naming [name].mjs --external fast-glob --external @iarna/toml",
    "build:cjs": "bun build src/index.ts src/cli.ts --target node --format cjs --outdir dist --entry-naming [name].cjs --external fast-glob --external @iarna/toml",
    "build:types": "tsup src/index.ts src/cli.ts --dts-only --format esm --out-dir dist",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint --write --unsafe .",
    "lint:check": "biome lint .",
    "check": "bun run typecheck && bun run format:check && bun run lint:check && bun test",
    "prepublishOnly": "bun run build"
  },
  "bin": {
    "textprompts": "./dist/cli.cjs"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "fast-glob": "^3.3.2"
  },
  "devDependencies": {
    "@ai-sdk/openai": "^1.0.0",
    "@biomejs/biome": "^2.2.5",
    "@types/bun": "^1.0.7",
    "@types/node": "^20.11.30",
    "ai": "^4.0.0",
    "openai": "^4.73.0",
    "tsup": "^7.2.0",
    "typescript": "^5.4.0"
  }
}
