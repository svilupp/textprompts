// Shared constants across the package

export const PLACEHOLDER_PATTERN = /\{([^}:]*)(?::[^}]*)?\}/g;
export const ESCAPED_OPEN = "\x00ESCAPED_OPEN\x00";
export const ESCAPED_CLOSE = "\x00ESCAPED_CLOSE\x00";
